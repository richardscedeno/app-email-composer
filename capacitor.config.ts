import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.jcedenio.emailComposer',
  appName: 'emailComposer',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
